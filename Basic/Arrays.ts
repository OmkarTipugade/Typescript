const superHeros: string[] = []
const heroPow : Array<number> = []
superHeros.push("karn")
heroPow.push(10)

type User = {
    username: string;
    email: string;
}

const allUsers : User[] = []

allUsers.push({username: "omkar", email: "om@dev.com"})

export{}